import { StyleSheet, View } from "react-native";

export default function SubAnimatedText() {
    return <View style={styles.container}>
        
    </View>
}

const styles = StyleSheet.create({
    container: {
        alignItems: 'center'
    }
});